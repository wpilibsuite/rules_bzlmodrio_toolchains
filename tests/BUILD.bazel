load("//:defines_helper.bzl", "defines_helper")

cc_binary(
    name = "main",
    srcs = ["main.cpp"],
    defines = select(defines_helper()),
)

cc_test(
    name = "test",
    srcs = ["test.cpp"],
    defines = select(defines_helper()),
    deps = [
        "@googletest//:gtest_main",
    ],
)
